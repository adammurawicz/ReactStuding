import s from './Header.module.css'

function Header () {
    return (
        <div className={s.header}>
            <p>My first React application with API</p>
        </div>
    )
}

export default Header